# ===========================================
# ILIYIN — Variables d'environnement
# ===========================================
# Copier en .env.local pour le dev local
# Ajouter dans Vercel → Settings → Environment Variables pour la prod

# === BASE DE DONNÉES (obligatoire) ===
# PostgreSQL (Vercel Postgres, Neon, Supabase, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"

# === SITE ===
# URL du site (pour OpenGraph, emails, etc.)
NEXT_PUBLIC_SITE_URL="https://association-iliyin.fr"

# === ADMIN ===
# Mot de passe pour accéder à /admin
ADMIN_PASSWORD="changeme"

# === EMAILS (Resend) ===
# Clé API Resend (https://resend.com/api-keys)
# Plan gratuit : 3 000 emails/mois
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx"

# Email expéditeur (par défaut: onboarding@resend.dev, ou ton domaine vérifié)
RESEND_FROM_EMAIL="onboarding@resend.dev"

# Email de notification (où l'asso reçoit les formulaires)
NOTIFICATION_EMAIL="assoiliyin@gmail.com"

# === ANALYTICS (optionnel) ===
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
