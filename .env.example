# ===========================================
# ILIYIN — Variables pour dev et Vercel
# ===========================================
# Copier en .env.local pour le dev local.

# Base de données PostgreSQL (obligatoire pour contact + health)
# — En local : PostgreSQL ou Docker
# — Sur Vercel : Vercel Postgres, Neon, Supabase, etc.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"

# URL du site (optionnel, pour liens absolus / emails)
NEXT_PUBLIC_SITE_URL="https://votre-projet.vercel.app"

# Admin (mot de passe pour /admin — à définir sur Vercel)
ADMIN_PASSWORD="votre-mot-de-passe-secret"

# Analytics (optionnel)
# NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
